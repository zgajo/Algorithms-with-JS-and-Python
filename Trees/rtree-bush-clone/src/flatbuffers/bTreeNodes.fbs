namespace Map.Node;

table BTreeLeafNode {
  id: short;
  lat: double;
  lon: double;
  part_of_ways: [short];
  points_to: [short];
  distance: [double];
}

table BTreeNode {
  keys: [short];
  values: [BTreeLeafNode];  
  is_shared: bool = false;
  children: [BTreeNode];
}

/** BTree of Nodes **/
table BNodesTree {
  size: short;
  max_node_size: short;
  root: BTreeNode;
}

root_type BNodesTree;